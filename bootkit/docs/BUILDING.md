# Bootkit Build Notes

Use the repository root build entry points:

```bat
..\scripts\build_bootkit.bat
```

Open `bootkit.sln` for only this EFI project, or open `..\root.sln` for both EFI projects.

The shared EDK II package metadata lives in `..\edk2`.
